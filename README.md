# Registration-form
This form is build using python framework.

Run the command:
pip install tkinter

This helps in creating the GUI elements using the widgets.

// It has been created as a start to python programming. 
// It requires the user to input the personal info and submit it.
// Once they click on the submit button, the terminal displays the message as form accepted.

